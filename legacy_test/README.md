# Legacy test artifacts

This folder contains older test scripts and sample data that were used prior to converting the project to a formal `pytest` test suite.

Modern tests live in `tests/` and are executed via:

```powershell
python -m pytest -q
```
